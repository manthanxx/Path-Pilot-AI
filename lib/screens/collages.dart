import 'package:flutter/material.dart';

class Collages extends StatelessWidget {
  const Collages({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Collages"),
      ),
    );
  }
}